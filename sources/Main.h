#pragma once
#include <sstream>
#include <fstream>
#include <vector>
#include "Database.h"

using namespace std;

class Main {
public:
	// method for processing the source program
	void process(string program);
};